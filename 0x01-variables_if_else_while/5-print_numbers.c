#include <stdio.h>

/**
 * main - Prints numbers that are below 10 (integers)
 *
 * Return: Always 0 no error
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}

